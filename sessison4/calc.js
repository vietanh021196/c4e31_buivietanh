const calc = (x, y, op) => {
    let result = 0;
    if (op === "+") {
        results = x + y;
    }else if (op === "-") {
        results = x - y;
    }else if (op === "*") {
        results = x * y;
    }else if (op === "/") {
        results = x / y;
    } 
    return result; // gặp return là dừng lại hết
};